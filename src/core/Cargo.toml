[package]
name = "ddtrace-core"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.20.0", features = ["extension-module"] }
once_cell = "1.18.0"

[build-dependencies]
pyo3-build-config = "0.20.0"

[lib]
name = "_hub"
path = "lib.rs"
crate-type = ["cdylib"]
