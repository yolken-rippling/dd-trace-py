[package]
name = "ddtrace-core"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.20.0", features = ["extension-module"] }

[build-dependencies]
pyo3-build-config = "0.20.0"

[lib]
name = "_core"
path = "lib.rs"
crate-type = ["cdylib"]
